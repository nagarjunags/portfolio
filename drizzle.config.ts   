import { config } from "dotenv";
import { defineConfig } from "drizzle-kit";

config();

export default defineConfig({
  out: "./drizzle",
  schema: "./src/db/schema.ts",
  connectionString: process.env.DATABASE_URL, // Add this to Vercel environment variables
});